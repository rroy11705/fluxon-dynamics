// File path: src/components/common/SocialLinks.tsx
import { SocialLink } from '@/types';

type SocialLinksProps = {
  links: SocialLink[];
};

export const SocialLinks = ({ links }: SocialLinksProps) => {
  console.log('links', links);

  return <></>;
};
